type Props = {
  className?: string;
  title: React.ReactNode;
  open: boolean;
  children: React.ReactNode;
  hoverRender?: any;
  onClick?: React.MouseEventHandler<HTMLDivElement>;
};

export type { Props };
